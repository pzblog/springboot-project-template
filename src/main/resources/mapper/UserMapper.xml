<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.project.dao.UserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.company.project.entity.User">
        <id column="id" property="id" />
        <result column="tb_username" property="tbUsername" />
        <result column="password" property="password" />
        <result column="nick_name" property="nickName" />
        <result column="sex" property="sex" />
        <result column="register_date" property="registerDate" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, tb_username, password, nick_name, sex, register_date
    </sql>

</mapper>
